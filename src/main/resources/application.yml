spring:
  thymeleaf:
    mode: HTML

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?serverTimezone=JST
    username: root
    password: rootpassword  messages:    fallback-to-system-locale: false  jpa:    show-sql: true
    hibernate:      ddl-auto: none